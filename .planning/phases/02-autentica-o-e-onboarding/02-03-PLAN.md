---
phase: 02-autentica-o-e-onboarding
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/mocks/handlers/auth.ts
  - src/mocks/handlers/schools.ts
  - src/mocks/handlers/index.ts
  - src/features/auth/__tests__/auth-flow.test.ts
  - README.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - "D-08: MSW register valida body PDR e classes[] professor"
    - "D-10: MSW expires_in 28800 student / 86400 teacher"
    - "D-03/D-04: Testes login cobrem credencial demo e 401"
    - "MSW register/login retorna 201/200 ou 401/422 conforme cenário"
    - "Vitest cobre login feliz, login erro, register student e teacher"
    - "README documenta credenciais demo CETI para UAT AUTH-04 refresh"
  artifacts:
    - path: src/mocks/handlers/auth.ts
      provides: "Handlers register/login completos"
    - path: src/features/auth/__tests__/auth-flow.test.ts
      provides: "Testes integração MSW"
  key_links:
    - from: src/features/auth/__tests__/auth-flow.test.ts
      to: src/mocks/handlers/auth.ts
      via: setupServer
      pattern: auth/login
---

<objective>
Completar handlers MSW de autenticação (register multi-turma, erros 422/401) e testes Vitest automatizados dos fluxos feliz/erro — fechando validação Nyquist da fase.

Purpose: Garantir contratos API estáveis e regressão automatizada antes da Fase 3.
Output: `npm run test` verde com cenários auth; README com contas demo.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-autentica-o-e-onboarding/02-RESEARCH.md
@.planning/phases/02-autentica-o-e-onboarding/02-02-SUMMARY.md
@doc/PDR_VisionEdu.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Mock users bleed entre testes | low | Reset in-memory store beforeEach no handler ou vitest |
| Demo password hardcoded | low | README marca como fictício; não usar senhas reais |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: MSW auth handlers completos</name>
  <files>src/mocks/handlers/auth.ts, src/mocks/handlers/schools.ts, src/mocks/handlers/index.ts</files>
  <read_first>src/mocks/handlers/auth.ts, src/mocks/data/ceti-seed.ts, doc/PDR_VisionEdu.md §11.1, 02-CONTEXT.md D-08</read_first>
  <action>
    Estender `auth.ts`:
    - POST `/api/v1/auth/login`: validar email/password contra seed + in-memory users; 401 `{ message: 'Credenciais inválidas' }`; 200 `{ access_token, expires_in: 28800|86400, user }` por role.
    - POST `/api/v1/auth/register`: body student ou teacher (classes array); 422 field errors; 201 + token; adicionar user ao Map in-memory.
    - Token mock: `mock.jwt.{role}.{timestamp}` suficiente para testes.
    `schools.ts`: GET retorna escolas e turmas CETI para dropdowns.
    Export handlers sem duplicar rotas.
  </action>
  <verify>npm run dev — curl ou fetch POST login com credencial README</verify>
  <acceptance_criteria>
    - auth.ts contém "auth/register"
    - auth.ts retorna status 401 para password errada em teste manual ou vitest
    - register teacher aceita classes array com mais de uma turma
  </acceptance_criteria>
  <done>API mock cobre register/login PDR</done>
</task>

<task type="auto">
  <name>Task 2: Vitest auth flows</name>
  <files>src/features/auth/__tests__/auth-flow.test.ts, vitest.setup.ts</files>
  <read_first>vitest.setup.ts, src/mocks/node.ts, src/services/auth.service.ts, 02-VALIDATION.md</read_first>
  <action>
    Criar `auth-flow.test.ts` com MSW node server:
    1. login student demo → access_token defined, expires_in 28800
    2. login wrong password → rejects/throws
    3. register new student unique email → 201 + token
    4. register teacher 2 classes → user.teacherClasses length 2
    Opcional: `auth-store.test.ts` persist expiresAt.
    Garantir vitest.setup importa handlers atualizados.
  </action>
  <verify>npm run test</verify>
  <acceptance_criteria>
    - auth-flow.test.ts contém "auth/login"
    - npm run test exit code 0
    - Pelo menos 4 test cases describe/it
  </acceptance_criteria>
  <done>Todos testes auth passam</done>
</task>

<task type="auto">
  <name>Task 3: README credenciais demo</name>
  <files>README.md</files>
  <read_first>README.md, src/mocks/data/ceti-seed.ts</read_first>
  <action>
    Seção **Contas demo (MSW)**:
    - Aluno: email/senha fictícios do seed
    - Professor: email/senha fictícios
    - Fluxo: home → login → dashboard
    - Sessão 8h aluno; como testar refresh
    - `npm run test` para regressão auth
  </action>
  <verify>npm run lint && npm audit</verify>
  <acceptance_criteria>
    - README.md contém "demo" e "login"
    - README lista email aluno demo explícito
  </acceptance_criteria>
  <done>Documentação UAT auth pronta</done>
</task>

</tasks>

<verification>
- [ ] `npm run test` — all pass
- [ ] `npm run build` — success
- [ ] `npm audit` — no high/critical unmitigated
</verification>

<success_criteria>
- ROADMAP plan 02-03: MSW auth + Vitest feliz/erro
- Phase 2 success criteria 1–4 testáveis via test + manual README
</success_criteria>

<output>
Após conclusão: `.planning/phases/02-autentica-o-e-onboarding/02-03-SUMMARY.md`
</output>
