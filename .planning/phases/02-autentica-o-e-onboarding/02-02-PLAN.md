---
phase: 02-autentica-o-e-onboarding
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/stores/auth-store.ts
  - src/lib/session-cookie.ts
  - src/middleware.ts
  - src/app/unauthorized/page.tsx
  - src/app/(student)/layout.tsx
  - src/app/(teacher)/layout.tsx
  - src/app/(student)/dashboard/page.tsx
  - src/app/(teacher)/dashboard/page.tsx
  - src/features/auth/components/auth-header.tsx
  - src/features/auth/components/student-dashboard-skeleton.tsx
  - src/features/auth/components/teacher-dashboard-skeleton.tsx
  - src/features/auth/components/login-form.tsx
  - src/features/auth/components/register-student-form.tsx
  - src/features/auth/components/register-teacher-form.tsx
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - AUTH-05
user_setup: []
must_haves:
  truths:
    - "D-09: Zustand persist + cookie visionedu_session espelhado"
    - "D-10: session_expires_at 8h aluno checado no boot"
    - "D-11: Middleware guards (auth)/(student)/(teacher) paths"
    - "D-12: Papel errado → /unauthorized Sem permissão"
    - "D-17: Login/register redirect direto ao dashboard"
    - "D-18: Dashboard aluno esqueleto Trilha/Atividades/Score"
    - "D-19: Dashboard professor esqueleto Turmas/Médias/Lacunas/Tempo real"
    - "D-20: Logout no header layouts autenticados"
    - "D-21: Erros API 401 mapeados para role=alert no login"
  artifacts:
    - path: src/middleware.ts
      provides: "Guards por route group"
    - path: src/stores/auth-store.ts
      provides: "Zustand persist sessão"
    - path: src/app/unauthorized/page.tsx
      provides: "403 acessível"
  key_links:
    - from: src/middleware.ts
      to: src/lib/session-cookie.ts
      via: readSessionCookie
      pattern: visionedu_session
---

<objective>
Sessão mock completa: Zustand persist, cookie para middleware, login/register redirecionam aos dashboards, guards por papel, página 403, logout no header.

Purpose: AUTH-03 (login+erros), AUTH-04 (8h persist), AUTH-05 (role guards).
Output: Fluxo ponta-a-ponta auth funcional em dev com MSW.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-autentica-o-e-onboarding/02-CONTEXT.md
@.planning/phases/02-autentica-o-e-onboarding/02-RESEARCH.md
@.planning/phases/02-autentica-o-e-onboarding/02-01-SUMMARY.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Session fixation | low | Novo token a cada login; clearSession no logout |
| Role escalation via cookie edit | medium | Middleware valida role no cookie assinado simples ou JSON parse + match path; rejeitar malformed |
| Auth bypass middleware | high | Matcher cobre (student) e (teacher); auth routes redirect se cookie válido |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Auth store e cookie sync</name>
  <files>src/stores/auth-store.ts, src/lib/session-cookie.ts</files>
  <read_first>02-CONTEXT.md (D-09, D-10), 02-RESEARCH.md, src/services/auth.service.ts</read_first>
  <action>
    `auth-store.ts`: zustand + persist middleware name `visionedu-auth`; state: accessToken, user, role, expiresAt.
    `setSession({ token, user, expiresIn })`: expiresAt = Date.now() + expiresIn*1000; chamar `setSessionCookie({ role, expiresAt, token })`.
    `clearSession`: remove localStorage persist + `clearSessionCookie()`.
    `isExpired()`: Date.now() > expiresAt → clear e return true.
    Cookie name `visionedu_session`; valor JSON base64url ou encodeURIComponent JSON { role, exp, token }.
    `session-cookie.ts`: set/clear/read helpers; max-age segundos até exp.
    Provider opcional `AuthHydration` no root layout client: on mount, se expired clear; se cookie sem store rehydrate.
  </action>
  <verify>npm run test -- src/stores/auth-store.test.ts (criar teste isExpired e setSession)</verify>
  <acceptance_criteria>
    - auth-store.ts contém "persist"
    - session-cookie.ts contém "visionedu_session"
    - expiresAt calculado com expires_in 28800 para student em teste
  </acceptance_criteria>
  <done>Store e cookie sincronizam no login</done>
</task>

<task type="auto">
  <name>Task 2: Middleware e 403</name>
  <files>src/middleware.ts, src/app/unauthorized/page.tsx</files>
  <read_first>02-CONTEXT.md (D-11, D-12), node_modules/next/dist/docs (middleware), src/app/(student)/layout.tsx</read_first>
  <action>
    `middleware.ts`:
    - Ler cookie; se ausente/expirado e path starts with /student ou /teacher (route groups mapeiam URLs sem prefixo group — usar pathname includes ou estrutura `(student)` → URLs reais conforme app dir: tipicamente rotas em `(student)/dashboard` → `/dashboard` NÃO — em App Router route groups não aparecem na URL. Usar segmentos: criar paths `/student/dashboard` e `/teacher/dashboard` OU manter `(student)/dashboard` → URL `/dashboard` conflita. **Decisão:** usar prefixos URL `/aluno` e `/professor` OU `(student)` folder com path `student/dashboard` → `src/app/(student)/student/dashboard` → `/student/dashboard`. CONTEXT diz dashboards `(student)/dashboard/page.tsx` → URL `/dashboard` only if single group. Verificar layout: melhor `src/app/(student)/dashboard` → URL `/dashboard` — conflito entre groups. **Implementar:** `src/app/student/dashboard` e `src/app/teacher/dashboard` sem route groups conflitantes OU middleware matcher `/student/:path*` e `/teacher/:path*`. Alinhar com 02-01: mover dashboards para `src/app/student/dashboard/page.tsx` e `src/app/teacher/dashboard/page.tsx` (sem parentheses) para URLs claras `/student/dashboard`, `/teacher/dashboard`.
    - `(auth)/*`: se cookie válido redirect `/student/dashboard` ou `/teacher/dashboard` por role.
    - Wrong role → redirect `/unauthorized`.
    `unauthorized/page.tsx`: título "Sem permissão", texto institucional, botão voltar ao painel correto.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - middleware.ts exporta function middleware
    - unauthorized/page.tsx contém "Sem permissão"
    - matcher ou lógica cobre student e teacher paths
  </acceptance_criteria>
  <done>Guards bloqueiam e permitem rotas corretas</done>
</task>

<task type="auto">
  <name>Task 3: Wire forms, dashboards, logout</name>
  <files>src/features/auth/components/login-form.tsx, register forms, src/app/(student)/dashboard/page.tsx, src/app/(teacher)/dashboard/page.tsx, layouts, auth-header.tsx, skeleton components</files>
  <read_first>02-CONTEXT.md (D-17 a D-21), 02-01 forms</read_first>
  <action>
    Login/register onSuccess: `useAuthStore.getState().setSession(...)` → `router.push` student ou teacher dashboard.
    Login error 401: setError root "E-mail ou senha incorretos" role=alert.
    `AuthHeader` em layouts student/teacher: nome usuário, botão Sair → clearSession + push /login.
    `StudentDashboardSkeleton`: saudação, escola/turma, 3 cards Trilha/Atividades/Score disabled "Em breve".
    `TeacherDashboardSkeleton`: turmas listadas, 4 cards Turmas/Médias/Lacunas/Tempo real "Em breve".
    Ajustar estrutura de pastas conforme Task 2 (`/student/dashboard`, `/teacher/dashboard`).
    Remover redirect temporário do 02-01.
  </action>
  <verify>Manual: login demo → refresh F5 ainda autenticado; visit /teacher/dashboard as student → 403</verify>
  <acceptance_criteria>
    - login-form.tsx importa useAuthStore ou setSession
    - student dashboard contém "Trilha" ou "Em breve"
    - teacher layout contém "Sair" ou logout handler
  </acceptance_criteria>
  <done>Success criteria Phase 2 auth flow manual pass</done>
</task>

</tasks>

<verification>
- [ ] Login demo persiste após reload
- [ ] `npm run lint` && `npm run build`
- [ ] Cross-role acessa /unauthorized
</verification>

<success_criteria>
- AUTH-03, AUTH-04, AUTH-05 verificáveis
- ROADMAP success criteria 2.3 e 2.4 atendidos
</success_criteria>

<output>
Após conclusão: `.planning/phases/02-autentica-o-e-onboarding/02-02-SUMMARY.md`
</output>
