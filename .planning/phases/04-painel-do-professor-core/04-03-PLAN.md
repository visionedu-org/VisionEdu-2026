---
phase: 04-painel-do-professor-core
plan: "03"
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - src/lib/validations/teacher.ts
  - src/lib/validations/teacher.test.ts
  - src/mocks/teacher-content-memory.ts
  - src/mocks/handlers/teachers.ts
  - src/services/teacher.service.ts
  - src/features/teacher/components/content-form.tsx
  - src/features/teacher/components/diagnostic-builder.tsx
  - src/app/teacher/conteudos/novo/page.tsx
  - src/app/teacher/diagnosticos/novo/page.tsx
requirements:
  - TEACH-03
  - TEACH-04
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-13: Form material com disciplina, série, turma, tipo"
    - "D-14: PDF upload só UI"
    - "D-16: Builder MCQ dinâmico 1-10 questões"
    - "D-17: Tag BNCC obrigatória por questão"
    - "D-15: POST contents → toast sucesso (material não redireciona share)"
    - "D-18: POST diagnóstico redirect compartilhar"
  artifacts:
    - path: src/lib/validations/teacher.ts
      provides: "Schemas Zod"
    - path: src/features/teacher/components/diagnostic-builder.tsx
      provides: "MCQ builder"
  key_links:
    - from: src/features/teacher/components/diagnostic-builder.tsx
      to: src/mocks/handlers/teachers.ts
      via: createActivity POST
      pattern: teacherService.createActivity
---

<objective>
Formulários de criação de material e avaliação diagnóstica MCQ com tags BNCC.

Purpose: TEACH-03, TEACH-04.
Output: Professor cria conteúdo ou diagnóstico e recebe activityId para compartilhar.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-painel-do-professor-core/04-CONTEXT.md
@src/lib/validations/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zod schemas + memory store</name>
  <files>src/lib/validations/teacher.ts, src/lib/validations/teacher.test.ts, src/mocks/teacher-content-memory.ts</files>
  <read_first>src/lib/validations/auth.ts, src/mocks/data/bncc-competencies.ts</read_first>
  <action>
    contentFormSchema: title min 3, description, subject enum, grade, class_identifier, contentType enum, optional videoUrl.
    diagnosticFormSchema: title, class fields, questions array min 1 max 10, each with prompt, 4 options, bnccCode from fixture list.
    teacher-content-memory.ts: Map activities/contents created; export addActivity() for MSW + merge into students GET /activities/:id.
    Tests: reject question without bnccCode.
  </action>
  <verify>npm run test -- src/lib/validations/teacher.test.ts</verify>
  <acceptance_criteria>
    - teacher.test.ts passa
    - teacher.ts exporta diagnosticFormSchema
  </acceptance_criteria>
  <done>Validação e persistência mock</done>
</task>

<task type="auto">
  <name>Task 2: MSW POST contents + activities</name>
  <files>src/mocks/handlers/teachers.ts, src/services/teacher.service.ts</files>
  <read_first>src/mocks/teacher-content-memory.ts, src/mocks/handlers/students.ts</read_first>
  <action>
    POST /api/v1/teachers/contents → 201 { id }
    POST /api/v1/teachers/activities → 201 { id, title, questions } — gera UUID, registra em memory + demoActivities para aluno abrir.
    teacherService.createContent, createActivity.
    Estender students GET /activities/:id para ler activities criadas pelo professor.
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - teachers.ts contém POST teachers/activities
    - students handler resolve activity criada em memory
  </acceptance_criteria>
  <done>CRUD mock criador</done>
</task>

<task type="auto">
  <name>Task 3: ContentForm + DiagnosticBuilder pages</name>
  <files>src/features/teacher/components/content-form.tsx, src/features/teacher/components/diagnostic-builder.tsx, src/app/teacher/conteudos/novo/page.tsx, src/app/teacher/diagnosticos/novo/page.tsx</files>
  <read_first>src/features/auth/components/register-student-form.tsx, src/hooks/use-ceti-options.ts</read_first>
  <action>
    ContentForm: react-hook-form + zod, selects CETI, tipo conteúdo condicional (link vídeo se video_link, file input label se pdf_upload).
    Submit → createContent → toast sucesso + link opcional.
    DiagnosticBuilder: FieldArray questões, add/remove buttons min-h-11, BNCC select por questão, submit → createActivity → router.push `/teacher/compartilhar/${id}`.
    Erros inline + role=alert.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - diagnosticos/novo/page.tsx renderiza DiagnosticBuilder
    - diagnostic-builder.tsx contém bnccCode field
  </acceptance_criteria>
  <done>Formulários criador</done>
</task>

</tasks>

<verification>
- [ ] Criar diagnóstico com 2 questões e tags BNCC
- [ ] Atividade criada abre no player aluno após login
</verification>
