---
phase: 04-painel-do-professor-core
plan: "04"
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - package.json
  - src/features/teacher/components/share-activity-panel.tsx
  - src/app/teacher/compartilhar/[activityId]/page.tsx
  - src/mocks/handlers/teachers.ts
  - README.md
  - src/features/teacher/__tests__/teacher-flow.test.ts
requirements:
  - TEACH-05
  - TEACH-06
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-19: URLs aluno atividade e entrar?code="
    - "D-20: Copiar link com aria-live"
    - "D-21: QR Code via lib qrcode"
    - "D-22: Atividades criadas abrem no fluxo aluno Fase 3"
  artifacts:
    - path: src/features/teacher/components/share-activity-panel.tsx
      provides: "Link + QR"
    - path: src/features/teacher/__tests__/teacher-flow.test.ts
      provides: "Testes MSW professor"
  key_links:
    - from: src/features/teacher/components/share-activity-panel.tsx
      to: /student/atividade/
      via: shareUrl string
      pattern: /student/atividade/
---

<objective>
Tela de compartilhamento com link copiável, QR Code e testes; documentação demo professor.

Purpose: TEACH-05; fecha integração com Fase 3.
Output: `/teacher/compartilhar/[activityId]` pronto para uso em aula.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-painel-do-professor-core/04-CONTEXT.md
@.planning/phases/03-experi-ncia-do-aluno-core/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar qrcode</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
    npm install qrcode && npm install -D @types/qrcode
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - package.json dependencies contém "qrcode"
  </acceptance_criteria>
  <done>Dep QR disponível</done>
</task>

<task type="auto">
  <name>Task 2: ShareActivityPanel</name>
  <files>src/features/teacher/components/share-activity-panel.tsx, src/app/teacher/compartilhar/[activityId]/page.tsx</files>
  <read_first>.planning/phases/04-painel-do-professor-core/04-CONTEXT.md (D-19 a D-21)</read_first>
  <action>
    Client component: recebe activityId, monta shareUrl = `${origin}/student/atividade/${id}`, altUrl entrar?code=.
    Botão Copiar: navigator.clipboard.writeText + span role=status "Link copiado".
    useEffect QRCode.toDataURL(shareUrl) → img alt="QR Code para abrir atividade no celular do aluno".
    Instruções PT-BR para professor projetar na aula.
    page.tsx valida UUID e activity existe (GET teachers/activities/:id ou memory).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - share-activity-panel.tsx contém QRCode ou toDataURL
    - compartilhar/[activityId]/page.tsx existe
    - Botão copiar com aria-live
  </acceptance_criteria>
  <done>Tela compartilhamento</done>
</task>

<task type="auto">
  <name>Task 3: README + teacher-flow tests</name>
  <files>README.md, src/features/teacher/__tests__/teacher-flow.test.ts</files>
  <read_first>src/features/student/__tests__/student-flow.test.ts, README.md</read_first>
  <action>
    README seção Demo professor Fase 4: credenciais regina, fluxo criar diagnóstico → compartilhar → abrir como aluno.
    Testes: getClassDashboard, getBnccGaps length ≥6, createActivity + getActivity roundtrip.
  </action>
  <verify>npm run test</verify>
  <acceptance_criteria>
    - teacher-flow.test.ts passa
    - README menciona /teacher/compartilhar/
  </acceptance_criteria>
  <done>Docs e testes</done>
</task>

</tasks>

<verification>
- [ ] QR renderiza e link copia
- [ ] Aluno abre URL compartilhada após login
- [ ] npm run test passa
</verification>
