---
phase: 01-fundacao-frontend-e-camada-mock
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/mocks/handlers/index.ts
  - src/mocks/handlers/health.ts
  - src/mocks/handlers/auth.ts
  - src/mocks/handlers/schools.ts
  - src/mocks/browser.ts
  - src/mocks/node.ts
  - src/mocks/utils.ts
  - src/components/providers/msw-provider.tsx
  - src/app/layout.tsx
  - src/components/dev/auth-service-smoke.tsx
  - src/app/page.tsx
  - README.md
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-03
user_setup: []
must_haves:
  truths:
    - "GET /api/v1/health retorna JSON mock com dev server + MSW"
    - "AuthService.login com credencial demo retorna access_token"
    - "MSW desligável via NEXT_PUBLIC_USE_MOCK=false"
  artifacts:
    - path: src/mocks/handlers/index.ts
      provides: "Agregação handlers"
    - path: src/mocks/browser.ts
      provides: "setupWorker"
    - path: src/components/providers/msw-provider.tsx
      provides: "Inicialização browser MSW"
  key_links:
    - from: src/components/providers/msw-provider.tsx
      to: src/mocks/browser.ts
      via: worker.start
      pattern: setupWorker
    - from: src/services/auth.service.ts
      to: src/mocks/handlers/auth.ts
      via: http.post /api/v1/auth/login
      pattern: /auth/login
---

<objective>
Integrar MSW v2 interceptando `/api/v1/*`, handlers health/auth/schools, provider no layout e smoke test de AuthService na home.

Purpose: Fechar INFR-02 e validar stack mock end-to-end antes da Fase 2.
Output: Dev server com API mock funcional e teste Vitest com MSW node.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundacao-frontend-e-camada-mock/01-CONTEXT.md
@.planning/phases/01-fundacao-frontend-e-camada-mock/01-RESEARCH.md
@doc/PDR_VisionEdu.md
@.planning/phases/01-fundacao-frontend-e-camada-mock/01-02-SUMMARY.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| MSW ativo em produção | medium | Guard `NEXT_PUBLIC_USE_MOCK === 'true'` antes de worker.start |
| CORS / path mismatch | low | Handlers usam path absoluto `/api/v1/...` igual apiClient |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Handlers MSW base</name>
  <files>src/mocks/handlers/health.ts, src/mocks/handlers/auth.ts, src/mocks/handlers/schools.ts, src/mocks/handlers/index.ts, src/mocks/utils.ts</files>
  <action>
    Instalar `msw` devDependency.
    `health.ts`: GET `/api/v1/health` → `{ status: 'ok', mock: true, timestamp }`.
    `auth.ts`: POST login/register usando ceti-seed; JWT mock string base64 (não precisa ser real); expiração 8h documentada.
    `schools.ts`: GET schools + classes do seed.
    `utils.ts`: `withLatency(handler, ms)` wrapper opcional default 0.
    Export `handlers` array em index.ts.
  </action>
  <verify>npm run dev — fetch('/api/v1/health') no browser console retorna ok</verify>
  <acceptance_criteria>
    - handlers/index.ts exporta handlers
    - auth.ts contém "auth/login"
  </acceptance_criteria>
  <done>Health endpoint responde via MSW</done>
</task>

<task type="auto">
  <name>Task 2: Browser e Node MSW</name>
  <files>src/mocks/browser.ts, src/mocks/node.ts, src/components/providers/msw-provider.tsx, vitest.setup.ts</files>
  <action>
    `browser.ts`: setupWorker(...handlers).
    `node.ts`: setupServer(...handlers) com listen em vitest.setup.
    `MswProvider`: 'use client'; useEffect start worker se NEXT_PUBLIC_USE_MOCK=true; loading state opcional.
    Envolver children em layout.tsx dentro MswProvider (dentro ThemeProvider).
    Vitest: teste `auth.service.test.ts` — login email demo → expect access_token defined.
  </action>
  <verify>npm run test</verify>
  <acceptance_criteria>
    - msw-provider.tsx contém "setupWorker" ou import de browser
    - vitest.setup importa node server
  </acceptance_criteria>
  <done>Testes Vitest auth passam com MSW node</done>
</task>

<task type="auto">
  <name>Task 3: Smoke UI AuthService</name>
  <files>src/components/dev/auth-service-smoke.tsx, src/app/page.tsx, README.md</files>
  <action>
    Client component `AuthServiceSmoke` (somente dev ou flag): botão "Testar login mock" chama AuthService.login com credencial README; exibe sucesso/erro acessível (aria-live).
    README: credenciais demo, como desligar mock, curl exemplo health.
    Remover ou guard behind `process.env.NODE_ENV === 'development'`.
  </action>
  <verify>npm run lint && npm run build</verify>
  <acceptance_criteria>
    - page.tsx importa AuthServiceSmoke ou similar
    - README contém "demo" e "NEXT_PUBLIC_USE_MOCK"
  </acceptance_criteria>
  <done>Build produção ok; smoke funciona em dev</done>
</task>

</tasks>

<verification>
- [ ] `npm run test` — auth MSW test pass
- [ ] `npm run dev` — smoke login mock OK
- [ ] `npm run analyze` documentado (herdado 01-01)
- [ ] `npm audit` — sem high/critical pendentes
</verification>

<success_criteria>
- Todos INFR-01 a INFR-04 verificáveis
- Success criteria ROADMAP Phase 1 atendidos
- Pronto para `/gsd-execute-phase 1` ou plano 02 Fase 2
</success_criteria>

<output>
Após conclusão: `.planning/phases/01-fundacao-frontend-e-camada-mock/01-03-SUMMARY.md`
</output>
