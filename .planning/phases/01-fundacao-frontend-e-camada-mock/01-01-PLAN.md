---
phase: 01-fundacao-frontend-e-camada-mock
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.mjs
  - .env.example
  - README.md
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/(auth)/layout.tsx
  - src/app/(student)/layout.tsx
  - src/app/(teacher)/layout.tsx
  - src/components/providers/theme-provider.tsx
  - src/components/ui/theme-toggle.tsx
  - components.json
autonomous: true
requirements:
  - INFR-01
user_setup: []
must_haves:
  truths:
    - "npm run dev inicia Next.js sem erro"
    - "Toggle light/dark altera tema da página inicial"
    - "Route groups (auth), (student), (teacher) existem"
  artifacts:
    - path: package.json
      provides: "Scripts dev, build, lint, analyze"
    - path: src/app/layout.tsx
      provides: "Roboto, ThemeProvider, header com toggle"
    - path: src/app/globals.css
      provides: "Tokens cores institucionais"
  key_links:
    - from: src/app/layout.tsx
      to: src/components/providers/theme-provider.tsx
      via: import ThemeProvider
      pattern: ThemeProvider
---

<objective>
Scaffold Next.js 15 na raiz com TypeScript, Tailwind 4, Roboto, tema light/dark, tokens institucionais, route groups vazios e bundle analyzer configurado.

Purpose: Base executável para MSW e features nas planos 01-02 e 01-03.
Output: App rodando com home VisionEdu e estrutura `src/` conforme CONTEXT D-05–D-08.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fundacao-frontend-e-camada-mock/01-CONTEXT.md
@.planning/phases/01-fundacao-frontend-e-camada-mock/01-RESEARCH.md
@.cursor/rules/validation-build.mdc
@.cursor/rules/performance.mdc
@.cursor/rules/accessibility.mdc
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Secrets em .env commitados | medium | Apenas `.env.example`; `.env.local` no `.gitignore` |
| Dependências vulneráveis | medium | `npm audit` após install; corrigir high/critical |
| FOUC / flash tema errado | low | `suppressHydrationWarning` no html com next-themes |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Inicializar Next.js na raiz</name>
  <files>package.json, tsconfig.json, next.config.ts, .gitignore</files>
  <action>
    Na raiz VisionEdu, criar app com:
    `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm`
    (usar flags non-interactive; se diretório não vazio, criar arquivos manualmente espelhando output oficial).
    Nome pacote: `visionedu-web`.
    Adicionar deps: `next-themes`, `clsx`, `tailwind-merge`.
    Adicionar devDep: `@next/bundle-analyzer`.
    Configurar `next.config.ts` com `withBundleAnalyzer` quando `ANALYZE=true`.
    Scripts: `"analyze": "cross-env ANALYZE=true npm run build"` (ou set env Windows-compatible).
    `.env.example`: `NEXT_PUBLIC_USE_MOCK=true`, `NEXT_PUBLIC_APP_URL=http://localhost:3000`.
  </action>
  <verify>npm install && npm run build</verify>
  <acceptance_criteria>
    - package.json contém "next" e script "dev"
    - tsconfig.json paths incluem "@/*": ["./src/*"]
  </acceptance_criteria>
  <done>Build de produção conclui sem erro</done>
</task>

<task type="auto">
  <name>Task 2: Design tokens, Roboto e tema</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/components/providers/theme-provider.tsx, src/components/ui/theme-toggle.tsx</files>
  <action>
    `globals.css`: variáveis CSS `--color-primary`, `--color-secondary`, backgrounds light/dark (azul #2563eb, verde #15803d — verificar contraste AA no body).
    `layout.tsx`: import Roboto from `next/font/google` weight 400/500/700; className na html/body.
    `ThemeProvider` client com `attribute="class"`, `defaultTheme="light"`, `enableSystem={false}`.
    `ThemeToggle` botão acessível (aria-label "Alternar tema claro ou escuro") no header.
    Instalar shadcn mínimo: `npx shadcn@latest init` + add button, input, label, dialog, form — components em `src/components/ui/`.
  </action>
  <verify>npm run dev — abrir localhost:3000, alternar tema, inspecionar contraste no DevTools</verify>
  <acceptance_criteria>
    - layout.tsx contém "Roboto" ou variável --font-roboto
    - theme-toggle.tsx contém aria-label
  </acceptance_criteria>
  <done>Toggle dark/light funciona sem erro de hidratação</done>
</task>

<task type="auto">
  <name>Task 3: Estrutura pastas e route groups</name>
  <files>src/app/(auth)/layout.tsx, src/app/(student)/layout.tsx, src/app/(teacher)/layout.tsx, src/app/page.tsx, README.md, src/features/.gitkeep, src/mocks/.gitkeep, src/services/.gitkeep, src/types/.gitkeep, src/lib/.gitkeep</files>
  <action>
    Criar layouts placeholder para `(auth)`, `(student)`, `(teacher)` com comentário "Fase 2+".
    Home `page.tsx`: título VisionEdu, subtítulo piloto CETI, link "Em breve: login".
    Pastas vazias com .gitkeep conforme D-06.
    README: como rodar dev, script analyze, meta bundle 300KB gzip, estrutura de pastas.
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - Existe src/app/(student)/layout.tsx
    - README.md menciona "analyze" ou "bundle"
  </acceptance_criteria>
  <done>lint passa; rotas groups resolvíveis pelo Next</done>
</task>

</tasks>

<verification>
- [ ] `npm run lint` sem erros críticos
- [ ] `npm run build` sucesso
- [ ] `npm run dev` — página inicial carrega
- [ ] Toggle tema funciona
</verification>

<success_criteria>
- INFR-01 estrutura `src/` e App Router estabelecidos
- Bundle analyzer documentado no README
- Paleta institucional e Roboto aplicados
</success_criteria>

<output>
Após conclusão: `.planning/phases/01-fundacao-frontend-e-camada-mock/01-01-SUMMARY.md`
</output>
