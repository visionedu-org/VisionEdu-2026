---
phase: 01-fundacao-frontend-e-camada-mock
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/types/domain.ts
  - src/types/schemas.ts
  - src/mocks/data/ceti-seed.ts
  - src/lib/api-client.ts
  - src/lib/utils.ts
  - src/services/auth.service.ts
  - src/services/index.ts
  - vitest.config.ts
  - vitest.setup.ts
autonomous: true
requirements:
  - INFR-03
  - INFR-04
user_setup: []
must_haves:
  truths:
    - "AuthService.login pode ser importado e tipado sem any"
    - "Seed CETI exporta school, classes, users demo com UUIDs fixos"
    - "apiClient usa baseUrl /api/v1 e respeita NEXT_PUBLIC_USE_MOCK"
  artifacts:
    - path: src/types/domain.ts
      provides: "Tipos User, Student, Teacher, School, Class, Activity"
    - path: src/mocks/data/ceti-seed.ts
      provides: "Dados piloto CETI"
    - path: src/lib/api-client.ts
      provides: "fetch wrapper"
  key_links:
    - from: src/services/auth.service.ts
      to: src/lib/api-client.ts
      via: apiClient.post
      pattern: apiClient
---

<objective>
Definir tipos de domínio alinhados ao PDR §9, seed CETI com UUIDs estáveis, cliente HTTP abstrato e AuthService stub tipado.

Purpose: Contrato estável para MSW (plano 01-03) e Fase 2 auth UI.
Output: Tipos + services + Vitest setup (sem testes MSW ainda).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fundacao-frontend-e-camada-mock/01-CONTEXT.md
@doc/PDR_VisionEdu.md
@.planning/phases/01-fundacao-frontend-e-camada-mock/01-01-SUMMARY.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| PII real em seeds | low | Apenas dados fictícios; emails `@escola.pi.gov.br` demo |
| JWT mock em logs | low | Não console.log tokens em produção; só dev |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Tipos de domínio PDR</name>
  <files>src/types/domain.ts, src/types/schemas.ts</files>
  <read_first>doc/PDR_VisionEdu.md</read_first>
  <action>
    Em `domain.ts` definir interfaces:
    - UserRole = 'student' | 'teacher' | 'admin'
    - User, School, Class, Student, Teacher, Content, Activity, StudentActivity, DiagnosticResult
    Campos conforme PDR §9.1 e diagrama ER (id uuid, role, xp, level, status not_started|in_progress|completed, questions_data unknown/json).
    `schemas.ts`: Zod opcional para LoginRequest, RegisterStudentRequest, RegisterTeacherRequest espelhando §11.1.
    Export barrel `src/types/index.ts`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - domain.ts contém "export type UserRole"
    - domain.ts contém "identified_gaps"
  </acceptance_criteria>
  <done>Typecheck passa</done>
</task>

<task type="auto">
  <name>Task 2: Seed CETI piloto</name>
  <files>src/mocks/data/ceti-seed.ts</files>
  <action>
    Constantes:
    - SCHOOL_CETI: name "CETI Luiz Ubiraci de Carvalho", city "Vila Nova do Piauí", gre "16ª"
    - CLASSES: 1º/2º/3º turmas A e B (ids UUID fixos documentados)
    - DEMO_USERS: aluno Thiago-like, professora Regina-like (senha não armazenada em plain text no seed — hash placeholder ou login mock aceita "demo123")
    Export helpers: `getSchoolById`, `getClassById`, `findUserByEmail`.
    Comentário no arquivo referenciando PDR §2.1 contexto.
  </action>
  <verify>node -e "require('./src/mocks/data/ceti-seed.ts')" ou import em teste Vitest simples</verify>
  <acceptance_criteria>
    - ceti-seed.ts contém "Luiz Ubiraci"
    - ceti-seed.ts exporta DEMO_USERS
  </acceptance_criteria>
  <done>Seed importável sem erro</done>
</task>

<task type="auto">
  <name>Task 3: apiClient e AuthService</name>
  <files>src/lib/api-client.ts, src/lib/utils.ts, src/services/auth.service.ts, src/services/index.ts, vitest.config.ts, vitest.setup.ts</files>
  <action>
    `api-client.ts`: classe ou funções `get/post` com prefixo `/api/v1`, headers JSON, lê `process.env.NEXT_PUBLIC_USE_MOCK` (flag para logging apenas na Fase 1).
    `auth.service.ts`: interface IAuthService + class AuthService com login/register retornando `{ access_token, user, profile }` tipado.
    Implementação usa apiClient (ainda sem MSW — testes no 01-03).
    Vitest + @testing-library/react configurados; script `"test": "vitest run"` no package.json.
    `utils.ts`: cn() com clsx + tailwind-merge.
  </action>
  <verify>npm run test -- --passWithNoTests ou teste unitário AuthService mock manual</verify>
  <acceptance_criteria>
    - auth.service.ts exporta AuthService
    - api-client.ts contém "/api/v1"
  </acceptance_criteria>
  <done>AuthService instancia sem erro de tipo</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
- [ ] Tipos cobrem entidades PDR §9
</verification>

<success_criteria>
- INFR-04 completo
- INFR-03 cliente HTTP pronto para MSW
- Seed CETI documentado
</success_criteria>

<output>
Após conclusão: `.planning/phases/01-fundacao-frontend-e-camada-mock/01-02-SUMMARY.md`
</output>
