---
phase: 07-gamifica-o-e-polimento
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - package.json
  - package-lock.json
  - .lighthouserc.cjs
  - scripts/measure-student-bundle.mjs
  - scripts/lhci-login.mjs
  - README.md
  - .planning/phases/07-gamifica-o-e-polimento/07-VERIFICATION.md
requirements:
  - PERF-01
  - PERF-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Student entry route /student/dashboard gzip JS ≤ 300KB documented with measured KB value"
    - "Lighthouse Performance score ≥ 0.9 on authenticated /student/dashboard in production build"
    - "No qrcode import in src/app/student/** or src/features/student/**"
  artifacts:
    - path: .lighthouserc.cjs
      provides: "LHCI collect + assert performance ≥90"
      contains: "categories:performance"
    - path: scripts/measure-student-bundle.mjs
      provides: "Automated gzip sum for student dashboard route"
    - path: .planning/phases/07-gamifica-o-e-polimento/07-VERIFICATION.md
      provides: "PERF-01/02 evidence table"
  key_links:
    - from: package.json
      to: .lighthouserc.cjs
      via: "perf:student" script
      pattern: "lhci autorun"
    - from: scripts/lhci-login.mjs
      to: http://localhost:3000/student/dashboard
      via: puppeteerScript in lighthouserc
      pattern: "thiago.demo"
---

## Phase Goal

**As a** product owner, **I want to** prove the student entry route meets bundle and Lighthouse performance gates, **so that** the MVP satisfies PDR RNF-001 and PERF requirements.

<objective>
Measure and gate student dashboard bundle size (≤300KB gzip) and Lighthouse Performance ≥90 after Phase 7 gamification UI lands.

Purpose: PERF-01 and PERF-02 with repeatable scripts, not ad-hoc guesses.
Output: measure-student-bundle.mjs, @lhci/cli@0.15.1, .lighthouserc.cjs, perf:student script, VERIFICATION.md metrics.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-gamifica-o-e-polimento/07-RESEARCH.md (Package Legitimacy Audit, Pattern 6–7)
@.planning/phases/07-gamifica-o-e-polimento/07-01-SUMMARY.md
@README.md
@doc/PDR_VisionEdu.md (RNF-001)

Baseline (2026-05-21): student routes rootMainFiles gzip ~168.3 KB — headroom before gamification; re-measure after 07-01.

Student entry route for PERF-01: `/student/dashboard` (post-login landing).

LHCI auth: use puppeteerScript — login thiago.demo@escola.pi.gov.br / senhaDemo123 before auditing dashboard (unauthenticated runs measure /login).
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependency | @lhci/cli install in devDependencies only |
| CI/local → production server | perf:student runs next start against production build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | npm package @lhci/cli | mitigate | Install exact version 0.15.1 per RESEARCH Package Legitimacy Audit (Approved); slopcheck OK |
| T-07-05 | Spoofing | Lighthouse URL | mitigate | puppeteerScript logs in as demo student before collect |
| T-07-06 | Denial of Service | perf:student local | accept | Dev-only script; not run in user-facing runtime |
| T-07-SC | Tampering | npm install | mitigate | devDependency only; no new runtime deps; verify package-lock after install |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Bundle measurement script and student-route guardrails</name>
  <files>scripts/measure-student-bundle.mjs, README.md</files>
  <read_first>README.md, package.json, .planning/phases/07-gamifica-o-e-polimento/07-RESEARCH.md (Pattern 6)</read_first>
  <action>
    Create scripts/measure-student-bundle.mjs (Node ESM): run after `npm run build`; read `.next/build-manifest.json` or Next 16 equivalent app-paths manifest; sum gzip sizes of JS assets loaded by route `/student/dashboard` (use Next route manifest keys matching student dashboard — document method in script header comment); print single line `STUDENT_DASHBOARD_GZIP_KB={n}` and exit 1 if n > 300.

    README section "Performance gates (Fase 7)": document commands `npm run build && node scripts/measure-student-bundle.mjs` and threshold 300KB gzip for /student/dashboard.

    Audit student tree: confirm zero imports of `qrcode` under src/app/student and src/features/student (if found, remove/refactor — qrcode stays teacher-only).

    Audit new gamification components use named lucide imports only (no `import * from "lucide-react"`).

    If measure script reports > 280KB after 07-01, apply discretion: `next/dynamic` lazy-load StudentProfileView only on /student/perfil (not dashboard) — do not add runtime deps.
  </action>
  <verify>
    <automated>npm run build && node scripts/measure-student-bundle.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0 when gzip sum ≤ 300
    - README documents bundle measurement steps
    - ripgrep `from \"qrcode\"` under src/app/student and src/features/student returns no matches
  </acceptance_criteria>
  <done>PERF-01 measurement automated and student routes free of qrcode</done>
</task>

<task type="auto">
  <name>Task 2: Lighthouse CI — install, config, authenticated collect</name>
  <files>package.json, package-lock.json, .lighthouserc.cjs, scripts/lhci-login.mjs</files>
  <read_first>.planning/phases/07-gamifica-o-e-polimento/07-RESEARCH.md (Pattern 7, Package Legitimacy Audit)</read_first>
  <action>
    npm install -D @lhci/cli@0.15.1 (exact version per RESEARCH).

    Add package.json scripts: `"lhci": "lhci autorun"`, `"perf:student": "npm run build && lhci autorun"`.

    Create .lighthouserc.cjs at repo root: ci.collect.startServerCommand = "npm run start"; startServerReadyPattern = "Ready" or "started server"; url = ["http://localhost:3000/student/dashboard"]; numberOfRuns = 3; settings.preset = "mobile" (document in VERIFICATION — aligns RNF-002 mobile-first); onlyCategories = ["performance"]; ci.assert.assertions["categories:performance"] = ["error", { minScore: 0.9 }]; upload.target = "filesystem", outputDir = ".planning/phases/07-gamifica-o-e-polimento/lhci".

    Create scripts/lhci-login.mjs exported for puppeteerScript: navigate to /login, fill email thiago.demo@escola.pi.gov.br and password senhaDemo123, submit, wait for URL containing /student/dashboard (adjust selectors to match src/app/(auth)/login/page.tsx inputs).

    Wire puppeteerScript path in .lighthouserc.cjs collect.puppeteerScript to scripts/lhci-login.mjs.

    Set NEXT_PUBLIC_USE_MOCK=true in collect env if needed for MSW (document choice in VERIFICATION assumption A6).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=require('path');const cfg=fs.readFileSync('.lighthouserc.cjs','utf8');if(!/categories:performance/.test(cfg)||!/minScore:\\s*0\\.9/.test(cfg))process.exit(1);const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));if(!pkg.devDependencies['@lhci/cli'])process.exit(1);if(!fs.existsSync('scripts/lhci-login.mjs'))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - @lhci/cli@0.15.1 appears in package.json devDependencies only
    - .lighthouserc.cjs asserts performance minScore 0.9
    - scripts/lhci-login.mjs exists for authenticated collect
  </acceptance_criteria>
  <done>PERF-02 repeatable Lighthouse gate configured</done>
</task>

<task type="auto">
  <name>Task 3: Record verification evidence</name>
  <files>.planning/phases/07-gamifica-o-e-polimento/07-VERIFICATION.md</files>
  <read_first>.planning/phases/07-gamifica-o-e-polimento/07-VALIDATION.md</read_first>
  <action>
    Create or update 07-VERIFICATION.md with tables:

    PERF-01: date, command, measured STUDENT_DASHBOARD_GZIP_KB, pass/fail vs 300KB, note on shared chunks vs analyzer.

    PERF-02: date, preset mobile, median performance score (0–100), pass/fail vs 90, auth method (puppeteerScript), NEXT_PUBLIC_USE_MOCK value.

    If LHCI fails auth in CI environment, document manual fallback: Chrome Lighthouse logged-in on /student/dashboard with screenshot path — mark as manual-only row, do not lower minScore.

    Link README Performance section to this file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/07-gamifica-o-e-polimento/07-VERIFICATION.md';if(!fs.existsSync(p))process.exit(1);const t=fs.readFileSync(p,'utf8');if(!/PERF-01/.test(t)||!/PERF-02/.test(t)||!/300/.test(t))process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - 07-VERIFICATION.md contains measured gzip KB and Lighthouse score
    - Both PERF requirements marked pass or fail with evidence
  </acceptance_criteria>
  <done>Performance gates documented for phase verify-work</done>
</task>

</tasks>

<verification>
- [ ] `node scripts/measure-student-bundle.mjs` reports ≤ 300 KB after build
- [ ] `npm run perf:student` completes with performance category ≥ 90 (wave/phase gate — not per-task verify)
- [ ] npm run lint passes
- [ ] npm audit — no unmitigated high/critical from @lhci/cli (document moderate transitive if any)
</verification>

<success_criteria>
- PERF-01: Documented gzip ≤ 300KB for /student/dashboard production build
- PERF-02: Lighthouse Performance ≥ 90 on authenticated student dashboard (mobile preset)
</success_criteria>

<output>
Create `.planning/phases/07-gamifica-o-e-polimento/07-02-SUMMARY.md` when done
</output>
