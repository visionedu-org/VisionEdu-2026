---
phase: 03-experi-ncia-do-aluno-core
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/types/domain.ts
  - src/types/index.ts
  - src/mocks/data/student-fixtures.ts
  - src/mocks/handlers/students.ts
  - src/mocks/handlers/index.ts
  - src/services/student.service.ts
  - src/features/student/components/learning-path-timeline.tsx
  - src/features/student/components/student-dashboard.tsx
  - src/app/student/dashboard/page.tsx
requirements:
  - STUD-01
  - STUD-02
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-01: Timeline horizontal com scroll lateral no dashboard"
    - "D-02: Nós com cor+ícone e aria-label por estado"
    - "D-03: Trilha embutida abaixo dos cards no dashboard"
    - "D-04: Toque módulo desbloqueado navega para atividade"
    - "D-19: Cards escola, score médio, atividades com dados mock"
    - "D-21: Sem placeholder Em breve Fase 3 nos cards principais"
    - "D-22: Saudação Olá, {primeiro nome} no header do dashboard"
  artifacts:
    - path: src/features/student/components/learning-path-timeline.tsx
      provides: "Trilha horizontal"
    - path: src/mocks/handlers/students.ts
      provides: "API mock dashboard e learning-path"
  key_links:
    - from: src/features/student/components/student-dashboard.tsx
      to: src/services/student.service.ts
      via: fetchDashboard()
      pattern: StudentService
---

<objective>
Dashboard aluno com cards de desempenho e trilha horizontal interativa, alimentados por MSW.

Purpose: STUD-01 e STUD-02.
Output: `/student/dashboard` funcional com ≥3 estados de módulo visíveis.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-experi-ncia-do-aluno-core/03-CONTEXT.md
@.planning/phases/03-experi-ncia-do-aluno-core/03-RESEARCH.md
@doc/PDR_VisionEdu.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Dados dashboard vazam outro aluno | low | MSW filtra por token mock do auth store |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Tipos e fixtures</name>
  <files>src/types/domain.ts, src/types/index.ts, src/mocks/data/student-fixtures.ts</files>
  <read_first>doc/PDR_VisionEdu.md (§9), src/types/domain.ts</read_first>
  <action>
    Estender domain.ts:
    - `LearningPathModuleStatus` = 'locked' | 'in_progress' | 'completed'
    - `LearningPathModule`: id, title, status, activityId?: string
    - `StudentDashboardData`: schoolName, grade, classIdentifier, averageScore, activitiesCompleted, activitiesTotal, pendingActivities[]
    - `Activity`, `ActivityQuestion`, `McqOption` para uso no 03-03
    Criar `student-fixtures.ts`: ≥5 módulos (≥3 status distintos), 2 atividades MCQ ids estáveis UUID, dashboard payload CETI.
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - domain.ts exporta LearningPathModule e StudentDashboardData
    - student-fixtures.ts contém pelo menos 3 status diferentes em módulos
  </acceptance_criteria>
  <done>Tipos e seed aluno definidos</done>
</task>

<task type="auto">
  <name>Task 2: MSW students + StudentService</name>
  <files>src/mocks/handlers/students.ts, src/mocks/handlers/index.ts, src/services/student.service.ts</files>
  <read_first>src/mocks/handlers/auth.ts, src/lib/api-client.ts, src/mocks/data/student-fixtures.ts</read_first>
  <action>
    Handlers:
    - GET /api/v1/students/me/dashboard → StudentDashboardData
    - GET /api/v1/students/me/learning-path → { modules: LearningPathModule[] }
    Registrar em handlers/index.ts.
    StudentService: getDashboard(), getLearningPath() usando apiClient.
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - students.ts contém http.get para students/me/dashboard
    - index.ts exporta student handlers no array
    - student.service.ts exporta getDashboard
  </acceptance_criteria>
  <done>API mock aluno disponível</done>
</task>

<task type="auto">
  <name>Task 3: LearningPathTimeline</name>
  <files>src/features/student/components/learning-path-timeline.tsx</files>
  <read_first>.planning/phases/03-experi-ncia-do-aluno-core/03-CONTEXT.md (D-01 a D-04), .cursor/rules/accessibility.mdc</read_first>
  <action>
    Client component props: modules, onModulePress(module).
    Layout: overflow-x-auto, flex row, snap-x, gap-4, px-4.
    Nó 48px círculo: Lock / animação pulse / CheckCircle2 por status.
    aria-label em português: "Módulo X, bloqueado|em progresso|concluído".
    onClick: se locked toast via callback; else router.push `/student/atividade/${activityId}`.
    Conector horizontal entre nós (div h-0.5 bg-border).
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - learning-path-timeline.tsx usa três estados distintos visualmente
    - Arquivo contém overflow-x-auto e aria-label
  </acceptance_criteria>
  <done>Trilha horizontal implementada</done>
</task>

<task type="auto">
  <name>Task 4: StudentDashboard page</name>
  <files>src/features/student/components/student-dashboard.tsx, src/app/student/dashboard/page.tsx</files>
  <read_first>src/features/auth/components/student-dashboard-skeleton.tsx, src/stores/auth-store.ts</read_first>
  <action>
    Substituir skeleton por StudentDashboard:
    - useEffect fetch StudentService.getDashboard + getLearningPath
    - Cards grid: escola/turma, score %, atividades x/y
    - Seção "Sua trilha" + LearningPathTimeline
    - Toast simples para módulo bloqueado (state ou alert role=status)
    - Loading skeleton enquanto fetch
    page.tsx renderiza StudentDashboard apenas.
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - dashboard/page.tsx importa StudentDashboard não skeleton
    - student-dashboard.tsx chama getDashboard
    - Não contém string "Em breve — Fase 3" nos cards de dados
  </acceptance_criteria>
  <done>Dashboard com trilha e cards mock</done>
</task>

</tasks>

<verification>
- [ ] Dashboard exibe escola, turma, score e contagem de atividades
- [ ] Timeline mostra bloqueado, em progresso e concluído
- [ ] Toque em módulo em progresso navega para atividade (404 ok até 03-03)
</verification>
