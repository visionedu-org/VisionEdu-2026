---
phase: 03-experi-ncia-do-aluno-core
plan: "01"
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - src/app/student/layout.tsx
  - src/app/student/atividades/page.tsx
  - src/app/student/perfil/page.tsx
  - src/features/student/components/student-bottom-nav.tsx
  - src/middleware.ts
requirements:
  - STUD-05
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-09: Bottom nav Início / Atividades / Perfil no layout student"
    - "D-10: Bottom nav oculta em /student/atividade/*"
    - "D-11: Alvos de toque ≥ 44px e aria-current na aba ativa"
    - "D-13: Deep link /student/atividade/[id] acessível com sessão student"
  artifacts:
    - path: src/features/student/components/student-bottom-nav.tsx
      provides: "Navegação inferior mobile"
    - path: src/app/student/atividades/page.tsx
      provides: "Lista atividades placeholder"
  key_links:
    - from: src/app/student/layout.tsx
      to: src/features/student/components/student-bottom-nav.tsx
      via: import
      pattern: StudentBottomNav
---

<objective>
Layout mobile do aluno com navegação inferior fixa e rotas auxiliares (atividades, perfil esqueleto). Prepara shell para dashboard e player.

Purpose: STUD-05 (estrutura touch-friendly) antes de conteúdo rico nos planos 03-02–04.
Output: Layout student completo sem bottom nav no player.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-experi-ncia-do-aluno-core/03-CONTEXT.md
@.planning/phases/03-experi-ncia-do-aluno-core/03-RESEARCH.md
@.planning/phases/02-autentica-o-e-onboarding/02-CONTEXT.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Nav trap no player | low | Hide nav via pathname check D-10 |
| Touch targets < 44px | medium | min-h-11 min-w-11 nos links da nav |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: StudentBottomNav</name>
  <files>src/features/student/components/student-bottom-nav.tsx</files>
  <read_first>src/app/student/layout.tsx, .cursor/rules/responsive-mobile.mdc, .planning/phases/03-experi-ncia-do-aluno-core/03-CONTEXT.md</read_first>
  <action>
    Criar `StudentBottomNav` client component:
    - Links: `/student/dashboard` (Início), `/student/atividades` (Atividades), `/student/perfil` (Perfil)
    - `usePathname()` para `aria-current="page"`
    - Ícones `lucide-react`: Home, ListChecks, User
    - Container fixo `bottom-0`, `border-t`, `pb-safe`, grid 3 colunas, cada link `min-h-11` flex center
    - `nav` com `aria-label="Navegação principal do aluno"`
    Retornar `null` se pathname match `/student/atividade/`
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - student-bottom-nav.tsx exporta StudentBottomNav
    - Arquivo contém href="/student/dashboard" e min-h-11
    - Arquivo contém condicional atividade para ocultar nav
  </acceptance_criteria>
  <done>Bottom nav componente criado</done>
</task>

<task type="auto">
  <name>Task 2: Layout e páginas auxiliares</name>
  <files>src/app/student/layout.tsx, src/app/student/atividades/page.tsx, src/app/student/perfil/page.tsx</files>
  <read_first>src/features/student/components/student-bottom-nav.tsx, src/features/auth/components/auth-header.tsx</read_first>
  <action>
    Atualizar `student/layout.tsx`: estrutura `flex flex-col min-h-screen`, main `flex-1 pb-20` (espaço nav), incluir StudentBottomNav após children.
    Criar `atividades/page.tsx`: título "Suas atividades", texto placeholder "Lista carregada no plano 03-02".
    Criar `perfil/page.tsx`: esqueleto "Perfil — XP e badges na Fase 7".
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - layout.tsx importa StudentBottomNav
    - atividades/page.tsx existe com heading atividades
    - perfil/page.tsx existe
  </acceptance_criteria>
  <done>Shell student navegável entre 3 abas</done>
</task>

<task type="auto">
  <name>Task 3: Middleware next param (deep link)</name>
  <files>src/middleware.ts</files>
  <read_first>src/middleware.ts, .planning/phases/03-experi-ncia-do-aluno-core/03-CONTEXT.md (D-13)</read_first>
  <action>
    Quando `!session` e rota student: redirect para `/login?next=${encodeURIComponent(pathname)}` em vez de só `/login`.
    Não alterar guards de role existentes.
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - middleware.ts contém next= na redirect sem sessão para rotas /student
  </acceptance_criteria>
  <done>Deep link preserva destino pós-login</done>
</task>

</tasks>

<verification>
- [ ] Aluno autenticado navega entre dashboard, atividades, perfil via bottom nav
- [ ] Rota `/student/atividade/test-id` não exibe bottom nav (página pode 404 até 03-03)
- [ ] `npm run lint` e `npm run build` sem erros
</verification>
