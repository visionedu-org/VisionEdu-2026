---
phase: 03-experi-ncia-do-aluno-core
plan: "03"
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - src/lib/activity-draft.ts
  - src/lib/activity-draft.test.ts
  - src/features/student/components/resume-draft-banner.tsx
  - src/features/student/components/activity-player.tsx
  - src/app/student/atividade/[id]/page.tsx
  - src/app/student/atividade/[id]/not-found.tsx
requirements:
  - STUD-03
  - STUD-04
  - STUD-05
autonomous: true
user_setup: []
must_haves:
  truths:
    - "D-05: localStorage para rascunho de atividade"
    - "D-06: Salvar a cada resposta ao selecionar alternativa MCQ"
    - "D-07: Chave visionedu:activity:{activityId} com payload answers/currentIndex/updatedAt"
    - "D-08: Banner continuar de onde parou + Recomeçar"
    - "D-15: Uma questão por tela"
    - "D-16: Radio group acessível"
    - "D-17: Barra progresso no topo"
    - "D-18: Enviar na última questão limpa draft"
    - "D-14: Atividade inexistente → página amigável"
  artifacts:
    - path: src/lib/activity-draft.ts
      provides: "Persistência rascunho"
    - path: src/features/student/components/activity-player.tsx
      provides: "Player MCQ"
  key_links:
    - from: src/features/student/components/activity-player.tsx
      to: src/lib/activity-draft.ts
      via: saveDraft on answer change
      pattern: visionedu:activity:
---

<objective>
Player de atividade MCQ com navegação anterior/próxima, autosave localStorage e restauração após reload.

Purpose: STUD-03, STUD-04; reforça STUD-05 no player.
Output: `/student/atividade/[id]` funcional com ≥5 questões mock.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-experi-ncia-do-aluno-core/03-CONTEXT.md
@.planning/phases/03-experi-ncia-do-aluno-core/03-RESEARCH.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS via draft JSON | low | Validar shape Zod antes de aplicar draft |
| Draft de outra atividade | low | Chave inclui activityId |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: activity-draft helper + tests</name>
  <files>src/lib/activity-draft.ts, src/lib/activity-draft.test.ts</files>
  <read_first>.planning/phases/03-experi-ncia-do-aluno-core/03-CONTEXT.md (D-07), src/lib/validations/auth.test.ts</read_first>
  <action>
    Implementar saveDraft, loadDraft, clearDraft com prefix `visionedu:activity:`.
    Tipo ActivityDraft: answers array, currentIndex number, updatedAt ISO string.
    Zod schema opcional para parse seguro.
    Testes: salvar e carregar; clear remove; corrupt JSON retorna null.
  </action>
  <verify>npm run test -- src/lib/activity-draft.test.ts</verify>
  <acceptance_criteria>
    - activity-draft.test.ts passa
    - activity-draft.ts exporta saveDraft e loadDraft
  </acceptance_criteria>
  <done>Helper draft testado</done>
</task>

<task type="auto">
  <name>Task 2: MSW GET activity + POST submit</name>
  <files>src/mocks/handlers/students.ts, src/services/student.service.ts</files>
  <read_first>src/mocks/data/student-fixtures.ts, src/mocks/handlers/students.ts</read_first>
  <action>
    GET /api/v1/activities/:id → Activity com questions_data ≥5 MCQ.
    POST /api/v1/students/activities/:id/submit → { score, status: completed }.
    StudentService: getActivity(id), submitActivity(id, answers).
    404 se id desconhecido no fixture map.
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - students.ts contém GET /api/v1/activities/
    - student.service.ts exporta getActivity e submitActivity
  </acceptance_criteria>
  <done>Endpoints atividade mock</done>
</task>

<task type="auto">
  <name>Task 3: ActivityPlayer + ResumeDraftBanner</name>
  <files>src/features/student/components/activity-player.tsx, src/features/student/components/resume-draft-banner.tsx</files>
  <read_first>.cursor/rules/accessibility.mdc, src/components/ui/button.tsx, src/lib/activity-draft.ts</read_first>
  <action>
    ResumeDraftBanner: role=region, botões Continuar (aplica draft) e Recomeçar (clearDraft).
    ActivityPlayer:
    - Fetch activity por id
    - Progress bar: (currentIndex+1)/total * 100, aria-valuenow
    - fieldset/legend por questão; radios min-h-11
    - onChange radio → saveDraft imediato
    - Footer fixo: Anterior desabilitado idx 0; Próxima ou Enviar no último
    - Enviar chama submitActivity + clearDraft + mensagem sucesso
  </action>
  <verify>npm run lint</verify>
  <acceptance_criteria>
    - activity-player.tsx contém fieldset e saveDraft
    - resume-draft-banner.tsx contém Continuar e Recomeçar
    - Botões footer com min-h-11
  </acceptance_criteria>
  <done>Player MCQ completo</done>
</task>

<task type="auto">
  <name>Task 4: Rota atividade e not-found</name>
  <files>src/app/student/atividade/[id]/page.tsx, src/app/student/atividade/[id]/not-found.tsx</files>
  <read_first>src/features/student/components/activity-player.tsx</read_first>
  <action>
    page.tsx server: validar param id formato uuid opcional; client wrapper ActivityPlayer.
    not-found.tsx: mensagem PT "Atividade não encontrada", link voltar dashboard.
    Header voltar (←) min 44px sem bottom nav (layout já oculta).
  </action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - atividade/[id]/page.tsx renderiza ActivityPlayer
    - not-found.tsx existe com texto não encontrada
  </acceptance_criteria>
  <done>Rota player publicada</done>
</task>

</tasks>

<verification>
- [ ] Aluno responde questões, recarrega, vê banner e continua
- [ ] Recomeçar limpa respostas
- [ ] Enviar na última questão conclui mock
- [ ] ID inválido mostra not-found amigável
</verification>
